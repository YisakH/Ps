#include <cstdio>
#include <vector>

using namespace std;

vector<vector<int>> recul(int N)
{
	if (N % 2 == 0) {
		return mul(recul)
	}
}

vector<vector<int>> mul(vector<vector<int>> a, vector<vector<int>> b) {

}

int main()
{
	int N;

	scanf("%d", &N);
}