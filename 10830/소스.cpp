#include <cstdio>
#include <vector>

using namespace std;

vector<vector<int>> cal(int N, int size)
{
	if (N % 2 == 0) {
		vector<vector<int>> v = cal(N / 2, size);
		

	}
}

vector<vector<int>> mul(vector<vector<int>> v, int size) {
	for (int i = 0; i < size; i++) {
		int sum = 0;
		for (int j = 0; j < size; j++) {

		}
	}
}